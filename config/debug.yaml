defaults:
- model: catboost
- job: debug

hydra:
  run:
    dir: output/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: output/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
  # job:
  #   chdir: true
  output_subdir: .hydra